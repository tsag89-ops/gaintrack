// src/config/firebase.web.ts
// Web/server stub — @react-native-firebase uses native modules unavailable in
// Node.js (expo export). Metro prefers .web.ts over .ts automatically.

export const auth = null as any;
export const db = null as any;
export type { FirebaseAuthTypes } from '@react-native-firebase/auth';

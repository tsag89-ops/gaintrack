# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native — generated by EAS Prebuild, do not commit
# (app.json is the source of truth; committing these causes expo-doctor CNG warning)
android/
ios/
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env.*
!.env.example

# EAS local credentials — never commit
credentials.json
*.keystore.jks

# Firebase credential files — real versions injected via EAS env var (file secret)
# The committed GoogleService-Info.plist is a placeholder only
google-services.json
# GoogleService-Info.plist is intentionally committed as a placeholder

# typescript
*.tsbuildinfo

app-example
